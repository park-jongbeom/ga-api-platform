name: Deploy AI Consultant Service

on:
  push:
    branches: [ main ]
    paths:
      - 'ga-ai-consultant-service/**'
      - 'ga-common/**'
      - 'ga-grpc-interface/**'
      - '.github/workflows/deploy-ai-consultant.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build AI Consultant Service
        working-directory: .
        run: ./gradlew :ga-ai-consultant-service:build -x test

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Image
        working-directory: .
        run: |
          docker build -f ga-ai-consultant-service/Dockerfile -t patrick5471/ga-ai-consultant-service:latest .
          for i in 1 2 3; do
            echo "Docker push attempt $i..."
            if docker push patrick5471/ga-ai-consultant-service:latest; then
              echo "Docker push succeeded"
              break
            fi
            if [ "$i" -eq 3 ]; then
              echo "Docker push failed after 3 attempts"
              exit 1
            fi
            sleep $((i * 10))
          done

      - name: Copy docker-compose.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          source: "docker-compose.yml"
          target: "/home/${{ secrets.SERVER_USER }}/ga-api-platform"
          
      - name: SSH Deploy
        uses: appleboy/ssh-action@v1.0.3
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
          ALLOWED_ORIGINS: ${{ secrets.ALLOWED_ORIGINS }}
          RATE_LIMIT_PER_MINUTE: ${{ secrets.RATE_LIMIT_PER_MINUTE }}
          RATE_LIMIT_PER_HOUR: ${{ secrets.RATE_LIMIT_PER_HOUR }}
          RATE_LIMIT_PER_DAY: ${{ secrets.RATE_LIMIT_PER_DAY }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          envs: DB_HOST,DB_PORT,DB_NAME,DB_USERNAME,DB_PASSWORD,OPENAI_API_KEY,JWT_SECRET,REDIS_PASSWORD,ALLOWED_ORIGINS,RATE_LIMIT_PER_MINUTE,RATE_LIMIT_PER_HOUR,RATE_LIMIT_PER_DAY
          script: |
            # 디렉토리 생성 (없는 경우)
            mkdir -p /home/${{ secrets.SERVER_USER }}/ga-api-platform
            cd /home/${{ secrets.SERVER_USER }}/ga-api-platform
            
            # .env 파일 생성 또는 업데이트 (환경 변수 저장)
            cat > .env << EOF
            DB_HOST=${DB_HOST}
            DB_PORT=${DB_PORT}
            DB_NAME=${DB_NAME}
            DB_USERNAME=${DB_USERNAME}
            DB_PASSWORD=${DB_PASSWORD}
            OPENAI_API_KEY=${OPENAI_API_KEY}
            JWT_SECRET=${JWT_SECRET}
            REDIS_PASSWORD=${REDIS_PASSWORD:-}
            ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
            RATE_LIMIT_PER_MINUTE=${RATE_LIMIT_PER_MINUTE:-10}
            RATE_LIMIT_PER_HOUR=${RATE_LIMIT_PER_HOUR:-100}
            RATE_LIMIT_PER_DAY=${RATE_LIMIT_PER_DAY:-500}
            EOF
            
            # Docker Compose 명령어 확인 (docker-compose 우선 사용)
            if command -v docker-compose &> /dev/null; then
              DOCKER_COMPOSE_CMD="docker-compose"
            elif command -v docker &> /dev/null && docker compose version &> /dev/null 2>&1; then
              DOCKER_COMPOSE_CMD="docker compose"
            else
              echo "Docker Compose를 찾을 수 없습니다. 설치가 필요합니다."
              exit 1
            fi
            
            echo "Using: $DOCKER_COMPOSE_CMD"
            
            # 기존 컨테이너 중지 및 제거 (포트 충돌 방지)
            echo "Stopping existing ga-ai-consultant-service container..."
            $DOCKER_COMPOSE_CMD --env-file .env stop ga-ai-consultant-service || true
            $DOCKER_COMPOSE_CMD --env-file .env rm -f ga-ai-consultant-service || true
            
            # 이미지 pull (재시도 로직 포함)
            echo "Pulling latest image for ga-ai-consultant-service..."
            MAX_ATTEMPTS=3
            ATTEMPT=1
            while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
              echo "Attempt $ATTEMPT of $MAX_ATTEMPTS: Pulling ga-ai-consultant-service..."
              if timeout 120 $DOCKER_COMPOSE_CMD --env-file .env pull ga-ai-consultant-service 2>&1; then
                echo "Successfully pulled ga-ai-consultant-service"
                break
              fi
              if [ $ATTEMPT -lt $MAX_ATTEMPTS ]; then
                WAIT_TIME=$((ATTEMPT * 10))
                echo "Pull failed. Waiting ${WAIT_TIME}s before retry..."
                sleep $WAIT_TIME
              else
                echo "Warning: Failed to pull ga-ai-consultant-service after $MAX_ATTEMPTS attempts. Continuing with existing image..."
              fi
              ATTEMPT=$((ATTEMPT + 1))
            done
            
            # 서비스 시작
            echo "Starting ga-ai-consultant-service..."
            $DOCKER_COMPOSE_CMD --env-file .env up -d ga-ai-consultant-service
            
            # 서비스 상태 확인
            echo "Checking service status..."
            sleep 5
            $DOCKER_COMPOSE_CMD --env-file .env ps ga-ai-consultant-service
            
            echo "Deployment completed successfully!"