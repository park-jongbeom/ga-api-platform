# 로컬 프로파일 통합 설정 (DB 접속 + 시드). .gitignore 대상. 실제 값은 application-local.yml.example 복사 후 채우기.
# 사용: ./gradlew bootRun --args='--spring.profiles.active=local'

spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://ls-584229d62cccd625a5fa723267dbdbc614b3b0e5.c9wi0gwweu9n.ap-northeast-2.rds.amazonaws.com:5432/ga_db?sslmode=require
    username: dbmasteruser
    password: w,*i.uAk7f^Gj;Gk`3O`oqZx3`0fj9Vm
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

# 로컬 전용: 테스트용 계정 시드 (선택)
app:
  seed-test-user:
    enabled: true
    email: test@example.com
    password: test1234Z
    full-name: "테스트 사용자"
  matching:
    weights:
      academic: 0.20
      english: 0.15
      budget: 0.15
      location: 0.10
      duration: 0.10
      career: 0.30
    vector-search:
      top-k: 20
  ai:
    gemini:
      # 환경변수 참조 (.env 파일 또는 시스템 환경변수)
      # 로컬 개발: .env 파일 생성하여 실제 API Key 입력
      # 예: GEMINI_API_KEY=AIzaSy...
      api-key: ${GEMINI_API_KEY}
      model: ${GEMINI_MODEL:gemini-2.0-flash}
      embedding-model: ${GEMINI_EMBEDDING_MODEL:gemini-embedding-001}
      api-url: https://generativelanguage.googleapis.com/v1beta
