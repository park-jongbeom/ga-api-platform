# 환경 정보 단일 관리: 이 파일을 .env 로 복사한 뒤 값만 채워 사용하세요.
# .env 는 Git에 포함되지 않으며, 배포 서버에서만 보관합니다.

# Spring 프로파일 (Docker 배포 시 lightsail 사용)
SPRING_PROFILES_ACTIVE=lightsail

# Lightsail PostgreSQL (ga-matching-api 가 DB 연결 시 사용)
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USERNAME=
DB_PASSWORD=

# Gemini API (RAG/매칭용). GitHub Actions 배포 시 Secrets GEMINI_API_KEY 가 서버 .env 에 반영됨
# GEMINI_API_KEY=
