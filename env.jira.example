# JIRA 로컬 환경 변수 예제
# 이 파일을 docs/jira/jira.env 로 복사한 뒤 실제 값으로 채우세요.
# docs/ 는 push 제외이므로 docs/jira/jira.env 는 원격에 올라가지 않습니다.
# 로컬 스크립트(reports/jira-report-local.sh, reports/jira-generate-report.py, reports/jira-report-if-needed.py 등)는
# 환경 변수가 없을 때 docs/jira/jira.env 를 참조합니다.

JIRA_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your_api_token
