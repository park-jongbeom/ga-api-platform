# ========================================
# Lightsail PostgreSQL 환경변수 설정 템플릿
# ========================================
# 사용 방법:
# 1. 이 파일을 .env.lightsail 로 복사
# 2. 실제 값으로 변경
# 3. source .env.lightsail (Linux/Mac) 또는 스크립트로 로드 (Windows)

# ----------------------------------------
# Lightsail PostgreSQL 연결 정보
# ----------------------------------------
DATABASE_URL=jdbc:postgresql://ls-xxxxxxxxxxxxx.xxxxxxx.us-east-1.rds.amazonaws.com:5432/postgres
DATABASE_USERNAME=dbmasteruser
DATABASE_PASSWORD=your_lightsail_master_password

# 선택사항: 연결 풀 크기 (Lightsail 플랜에 맞게 조정)
DB_MAX_POOL_SIZE=5
DB_MIN_IDLE=2

# 선택사항: Flyway 스키마 (기본값: public)
# ai_consultant 스키마를 사용하려면 주석 해제
# FLYWAY_SCHEMA=ai_consultant

# ----------------------------------------
# OpenAI API 설정
# ----------------------------------------
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4

# ----------------------------------------
# JWT 보안 설정
# ----------------------------------------
# 주의: 256비트 (32바이트) 이상의 강력한 시크릿 키 사용
JWT_SECRET=your-256-bit-secret-key-for-jwt-validation-change-this-in-production

# ----------------------------------------
# Redis 설정 (Chat Memory & Rate Limiting)
# ----------------------------------------
REDIS_HOST=your-redis-host-or-ip
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# ----------------------------------------
# Rate Limiting 설정
# ----------------------------------------
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_HOUR=100
RATE_LIMIT_PER_DAY=500

# ----------------------------------------
# CORS 설정
# ----------------------------------------
ALLOWED_ORIGINS=https://app.goalmond.com,https://admin.goalmond.com

# ----------------------------------------
# 서버 설정
# ----------------------------------------
SERVER_PORT=8083

# ----------------------------------------
# CloudWatch 모니터링 (선택사항)
# ----------------------------------------
# CLOUDWATCH_ENABLED=true
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
