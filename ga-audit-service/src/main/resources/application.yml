spring:
  application:
    name: ga-audit-service
  
  # 가상 스레드 활성화
  threads:
    virtual:
      enabled: true
  
  # 데이터베이스 설정
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ga_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true

server:
  port: 8083

# gRPC 서버 설정
grpc:
  server:
    port: 9091

logging:
  level:
    root: INFO
    com.goalmond: DEBUG

# SpringDoc OpenAPI (Swagger) 설정
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  info:
    title: GA Audit Service API
    description: 감사 로그 서비스 API 문서
    version: 1.0.0
